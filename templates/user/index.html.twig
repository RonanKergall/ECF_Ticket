{% extends 'base.html.twig' %}

{% block title %}Hello UserController!{% endblock %}

{% block body %}
    Tickets :
    
    <ul>
        {% for ticket in allTickets %}
            <li> <a href="{{ path('ticket', {slug: ticket.id}) }}">{{ ticket.title }}</a> ({{ ticket.idCategory.libelle }}) by {{ ticket.idUser.email}}</li>
        {% endfor %}
    </ul>

{% endblock %}
